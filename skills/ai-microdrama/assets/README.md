# Local Source Materials

The original `assets/source-materials/` library is intentionally not committed because it is several gigabytes and contains files larger than GitHub's normal file-size limit.

Use the indexed references under `references/` for public repository workflows, and keep the large source library locally when running the skill on this machine.
